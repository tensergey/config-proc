server:
  port: ${PORT:8761}

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    enable-self-preservation: false

logging:
  file: /var/log/eureka.log
  path: /var/log/